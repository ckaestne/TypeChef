frontend project for linux

provides several additional sbt commands:

parse               parses a file
typecheck           parses and typechecks a file
process-file-fist   determines which files are relevant for the current partila configuration
stats               collects parser statistics on a file or file list
pcpp-stats          collects preprocessor errors on a file or file list
web                 executes parser and typechecker on a file and produces HTML output

