void doprnt_1 (int i) {
    cclen = (  len );
    if (zero_flag)
        pad = '0';
    pad = ' ';
#ifdef A
    if (minlen > cclen && !minus_flag)
#endif
    {
        int to_add = minlen - cclen;
        while (to_add > 0)
        {
            to_add--;
        }
    }
    if (maxlen >= 0)
        len = 1;
}
