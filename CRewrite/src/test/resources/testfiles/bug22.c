#if !definedEx(CONFIG_GUNZIP)
int gzip_main (void) {
    int a;
#if definedEx(CONFIG_FEATURE_GZIP_LONG_OPTIONS)
    int b;
#endif
    int c;
    do {
        int d;
    } while (0);
    int e;
    return e;
}
#endif
