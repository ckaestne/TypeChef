package de.fosd.typechef.featureexpr

import org.junit._

/**
 * Created by IntelliJ IDEA.
 * User: kaestner
 * Date: 06.01.11
 * Time: 17:43
 * To change this template use File | Settings | File Templates.
 */


class CNFBenchmark {


    @Test
    def testBenchSat1() = {
        println("starting...")
        val start = System.currentTimeMillis
        val result = largeExpression.isSatisfiable
        val stop = System.currentTimeMillis
        println("done. " + (stop - start) + " ms")
        println(result)
    }


    val CONFIG_X86_64 = FeatureExprFactory.createDefinedExternal("CONFIG_X86_64")
    val CONFIG_HIBERNATION = FeatureExprFactory.createDefinedExternal("CONFIG_HIBERNATION")
    val CONFIG_NEED_MULTIPLE_NODES = FeatureExprFactory.createDefinedExternal("CONFIG_NEED_MULTIPLE_NODES")
    val CONFIG_X86_LOCAL_APIC = FeatureExprFactory.createDefinedExternal("CONFIG_X86_LOCAL_APIC")
    val CONFIG_SMP = FeatureExprFactory.createDefinedExternal("CONFIG_SMP")
    val CONFIG_DEBUG_HIGHMEM = FeatureExprFactory.createDefinedExternal("CONFIG_DEBUG_HIGHMEM")
    def not(e: FeatureExpr) = e.not
    val largeExpression =
        ((((((((((((((((((((((((((((((((((((((((((((not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
        )
            and (((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
        )
            )
            or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )
            and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_NEED_MULTIPLE_NODES)
            or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
            )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
            and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and not(CONFIG_NEED_MULTIPLE_NODES)
            )
            or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC)
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            )
        )
            and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
        )
            and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
            and CONFIG_SMP)
            )


    /*((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not(((((((((((((((((((((((((((((((((((((((((((((((((((((((((((((((((not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and (((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and not(((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
     or ((((CONFIG_X86_64 or CONFIG_HIBERNATION)
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
     or (not((CONFIG_X86_64 or CONFIG_HIBERNATION)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
     and ((not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and not((not((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_NEED_MULTIPLE_NODES)
     or (CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and ((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not(CONFIG_NEED_MULTIPLE_NODES)
     and not(((CONFIG_DEBUG_HIGHMEM and CONFIG_X86_LOCAL_APIC and CONFIG_SMP and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and not(CONFIG_NEED_MULTIPLE_NODES)
    )
     or (CONFIG_DEBUG_HIGHMEM and CONFIG_NEED_MULTIPLE_NODES and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC)
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
    )
    )
     and not((CONFIG_X86_LOCAL_APIC and CONFIG_NEED_MULTIPLE_NODES)
    )
     and CONFIG_X86_LOCAL_APIC and not(CONFIG_NEED_MULTIPLE_NODES)
     and CONFIG_SMP)
    )
    )
    )
    */


    val stupidFormula = {
        val a = FeatureExprFactory.createDefinedExternal("a")
        val b = FeatureExprFactory.createDefinedExternal("b")
        var result: FeatureExpr = a
        // XXX: Now this is simplified during construction. Is still there a point in benchmarking this? PG
        for (i <- 1 until 100)
            result = (result andNot b) or b
        result
    }

    @Test def testBench2 = {
        println("starting...")
        val start = System.currentTimeMillis
        val f = stupidFormula
        println("created...")
        val v = f.isSatisfiable()
        val stop = System.currentTimeMillis
        println("done. " + (stop - start) + " ms")
        println(v)
    }

}